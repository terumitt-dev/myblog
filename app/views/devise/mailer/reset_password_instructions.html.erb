<%# Rails の default_url_options を一次ソースにし、テスト/開発環境でも壊れず、 %>
<%# APP_HOST に https:// が混ざっていても二重スキームにならない形で URL を組み立てる %>
<% url_options = Rails.application.config.action_mailer.default_url_options || {} %>
<% host = url_options[:host] || ENV.fetch('APP_HOST', 'localhost:3000') %>
<% protocol = url_options[:protocol] || 'https' %>
<% reset_url = "#{protocol}://#{host}/password/edit?reset_password_token=#{ERB::Util.url_encode(@token)}" %>

<p><%= t('devise.mailer.reset_password_instructions.greeting', recipient: @resource.email) %></p>

<p><%= t('devise.mailer.reset_password_instructions.instruction') %></p>

<p><a href="<%= reset_url %>"><%= t('devise.mailer.reset_password_instructions.action') %></a></p>

<p><%= t('devise.mailer.reset_password_instructions.instruction_2') %></p>
<p><%= t('devise.mailer.reset_password_instructions.instruction_3') %></p>
